[project]
name = "protest-events"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai",
    "pandas",
    "numpy",
    "sentence-transformers",
    "einops",
    "chonkie[token]",
    "requests",
]

[tool.uv]
dev-dependencies = []
